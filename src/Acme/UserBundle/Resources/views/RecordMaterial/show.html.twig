<h1>RecordMaterial</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Dia_regreso</th>
            <td>{{ entity.diaregreso|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Dia_regresado</th>
            <td>{{ entity.diaregresado|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Comentario</th>
            <td>{{ entity.comentario }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('recordmaterial') }}">
            Regresar a la lista
        </a>
    </li>
    <li>
        <a href="{{ path('recordmaterial_edit', { 'id': entity.id }) }}">
            Editar
        </a>
    </li>
    <li>
        <form action="{{ path('recordmaterial_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Eliminar</button>
        </form>
    </li>
</ul>
