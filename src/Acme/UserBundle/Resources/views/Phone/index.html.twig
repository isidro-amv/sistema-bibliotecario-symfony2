<h1>Phone list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Celular</th>
            <th>Telefono_1</th>
            <th>Telefono_2</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('phone_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.celular }}</td>
            <td>{{ entity.telefono1 }}</td>
            <td>{{ entity.telefono2 }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('phone_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('phone_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('phone_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
